To run this example, you will not need to install anything, but python itslef.

Run: `python hello.py`  
and open `http://localhost:8080/World` on your browser. 

Replace World for something else and see what happens.
